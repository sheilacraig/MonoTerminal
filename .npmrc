# 国内网络加速：Electron 运行时与 electron-builder 打包工具链走国内镜像。
# 不配置时，npm install / npm run dist:* 会从 GitHub 下载上百 MB 的二进制，
# 在国内网络下经常超时失败（ECONNRESET / ETIMEDOUT），表现为「源码装不上」。
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
