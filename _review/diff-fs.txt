diff --git a/electron/main.cjs b/electron/main.cjs
index 186dbcc..099a22f 100644
--- a/electron/main.cjs
+++ b/electron/main.cjs
@@ -142,6 +142,14 @@ if (!gotTheLock) {
           finished = true;
           clearInterval(interval);
           onReady();
+        } else if (!finished && res.statusCode && res.statusCode >= 500 && attempts >= maxRetries) {
+          finished = true;
+          clearInterval(interval);
+          if (onFailed) {
+            onFailed();
+          } else {
+            onReady();
+          }
         }
       });
 
diff --git a/server/aiService.ts b/server/aiService.ts
index 40de6e9..7816bbe 100644
--- a/server/aiService.ts
+++ b/server/aiService.ts
@@ -21,6 +21,11 @@ export interface OpsContext {
   currentUser?: string;
   osInfo?: string;
   commandHistory?: string[];
+  failedCommand?: {
+    command?: string;
+    exitCode: number;
+    output?: string;
+  };
 }
 
 export class AIService {
@@ -39,11 +44,18 @@ export class AIService {
 - 褰撳墠宸ヤ綔鐩綍: ${opsContext.currentDir || '/etc/nginx'}
 - 鎿嶄綔绯荤粺鐢诲儚: ${opsContext.osInfo || 'Linux x86_64 Ubuntu 22.04 LTS'}
 ${
-  opsContext.terminalSnippet
+  opsContext.failedCommand
     ? `
+- 寮傚父鍛戒护鐢熷懡鍛ㄦ湡 (OSC 133 璇箟鎰熺煡):
+  * 鎵ц鍛戒护: ${opsContext.failedCommand.command || '(鏈煡鍛戒护)'}
+  * 閫€鍑虹姸鎬佺爜: ${opsContext.failedCommand.exitCode}
+  * 鍛戒护闅旂杈撳嚭:\n\`\`\`text\n${opsContext.failedCommand.output || opsContext.terminalSnippet || ''}\n\`\`\`
+`
+    : opsContext.terminalSnippet
+      ? `
 - 缁堢鏈€杩戣緭鍑?(鑳屾櫙涓婁笅鏂?:\n\`\`\`text\n${opsContext.terminalSnippet}\n\`\`\`
 `
-    : ''
+      : ''
 }
 ================================
 `;
diff --git a/server/index.ts b/server/index.ts
index 1a3959d..a5e0f21 100644
--- a/server/index.ts
+++ b/server/index.ts
@@ -13,6 +13,7 @@ import { guardrailRouter } from './routes/guardrail';
 import { securityRouter } from './routes/security';
 import { setupWsRouter } from './ws/wsRouter';
 import { generateAuthToken, createAuthMiddleware, isAllowedOrigin, AuthContext } from './auth';
+import { errorMessage } from '../shared/errors';
 
 /** Preferred port; if taken we fall back to the next free one instead of crashing. */
 const PREFERRED_PORT = parseInt(process.env.PORT || '3001', 10);
@@ -113,6 +114,12 @@ async function main(): Promise<void> {
   app.use('/api/guardrail', guardrailRouter);
   app.use('/api/security', securityRouter);
 
+  // Global API JSON error handler: ensure JSON response instead of Express HTML stack trace
+  app.use((err: unknown, _req: express.Request, res: express.Response, _next: express.NextFunction) => {
+    console.error('[MonoTerminal Server Error]', err);
+    res.status(500).json({ success: false, error: errorMessage(err) });
+  });
+
   // WebSocket RPC Router (handshake enforces the same auth boundary)
   setupWsRouter(wss, aiService, authContext);
 
diff --git a/server/localFsManager.ts b/server/localFsManager.ts
index 1f38895..08cd9a6 100644
--- a/server/localFsManager.ts
+++ b/server/localFsManager.ts
@@ -73,13 +73,24 @@ export class LocalFsManager implements LocalFsManagerApi {
   }
 
   public readFile(filePath: string): string {
-    return fs.readFileSync(expandHome(filePath), 'utf8');
+    const target = expandHome(filePath);
+    const stats = fs.statSync(target);
+    const MAX_READ_SIZE = 10 * 1024 * 1024; // 10MB
+    if (stats.size > MAX_READ_SIZE) {
+      throw new Error(
+        `鏂囦欢杩囧ぇ (${(stats.size / 1024 / 1024).toFixed(1)}MB)锛屽湪绾跨紪杈戞渶澶ф敮鎸?10MB锛岃浣跨敤涓嬭浇鏌ョ湅`
+      );
+    }
+    return fs.readFileSync(target, 'utf8');
   }
 
   public writeFile(filePath: string, content: string): void {
     const target = expandHome(filePath);
-    fs.mkdirSync(path.dirname(target), { recursive: true });
-    fs.writeFileSync(target, content, 'utf8');
+    const dir = path.dirname(target);
+    fs.mkdirSync(dir, { recursive: true });
+    const tmp = path.join(dir, `.${path.basename(target)}.${Date.now()}.${Math.random().toString(36).slice(2)}.tmp`);
+    fs.writeFileSync(tmp, content, 'utf8');
+    fs.renameSync(tmp, target);
   }
 
   public delete(targetPath: string, _isDirectory: boolean): void {
diff --git a/server/sshManager.ts b/server/sshManager.ts
index d533e5e..daee1a6 100644
--- a/server/sshManager.ts
+++ b/server/sshManager.ts
@@ -106,6 +106,7 @@ export class SshManager {
       client.on('close', () => {
         instance.isAlive = false;
         events.emit('close');
+        this.closeSession(sessionId);
       });
 
       // Connect config with TCP keepalive
@@ -197,6 +198,17 @@ export class SshManager {
     const session = this.sessions.get(sessionId);
     if (!session || !session.sftp) throw new Error('SFTP 鏈氨缁?);
 
+    const stats = await new Promise<{ size: number }>((resolve, reject) => {
+      session.sftp!.stat(filePath, (err, stats) => (err ? reject(err) : resolve(stats)));
+    });
+
+    const MAX_READ_SIZE = 10 * 1024 * 1024; // 10MB
+    if (stats.size > MAX_READ_SIZE) {
+      throw new Error(
+        `鏂囦欢杩囧ぇ (${(stats.size / 1024 / 1024).toFixed(1)}MB)锛屽湪绾跨紪杈戞渶澶ф敮鎸?10MB锛岃浣跨敤涓嬭浇鏌ョ湅`
+      );
+    }
+
     return new Promise((resolve, reject) => {
       const chunks: Buffer[] = [];
       const readStream = session.sftp!.createReadStream(filePath);
@@ -210,12 +222,33 @@ export class SshManager {
     const session = this.sessions.get(sessionId);
     if (!session || !session.sftp) throw new Error('SFTP 鏈氨缁?);
 
-    return new Promise((resolve, reject) => {
-      const writeStream = session.sftp!.createWriteStream(filePath);
+    const sftp = session.sftp;
+    const lastSlash = filePath.lastIndexOf('/');
+    const dir = lastSlash >= 0 ? filePath.substring(0, lastSlash) : '';
+    const base = lastSlash >= 0 ? filePath.substring(lastSlash + 1) : filePath;
+    const tmpPath = `${dir ? dir + '/' : ''}.${base}.${Date.now()}.${Math.random().toString(36).slice(2)}.tmp`;
+
+    // 1. Write content to temporary file
+    await new Promise<void>((resolve, reject) => {
+      const writeStream = sftp.createWriteStream(tmpPath);
       writeStream.on('close', () => resolve());
       writeStream.on('error', (err: Error) => reject(err));
       writeStream.end(content, 'utf-8');
     });
+
+    // 2. Rename temporary file to target file
+    await new Promise<void>((resolve, reject) => {
+      sftp.unlink(filePath, () => {
+        sftp.rename(tmpPath, filePath, err => {
+          if (err) {
+            sftp.unlink(tmpPath, () => {});
+            reject(err);
+          } else {
+            resolve();
+          }
+        });
+      });
+    });
   }
 
   private async rmdirRecursive(sftp: SFTPWrapper, dirPath: string): Promise<void> {
